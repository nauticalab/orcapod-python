coverage:
  status:
    project:
      default:
        target: 70
        threshold: 5  # allow up to 5% drop from base before failing
    patch:
      default:
        target: 80
        threshold: 5
